version: '3'
services:
  ui:
    build:
      context: ./client
    volumes:
      - client_build:/opt/app/build
      - ./client:/opt/app
    ports:
      - "5000:5000"
    depends_on:
      - api

  api:
    tty: true
    build:
      context: ./server
    environment:
      DB_NAME: spades_dev
      DB_USER: spades
      DB_PASS: spades
      DB_HOST: db
      PASSWORD_SALT: 1234
      TERM: "xterm-256color"
    volumes:
      - server_build:/opt/app/build
      - ./server:/opt/app
    ports:
      - "4000:4000"
    depends_on:
      - db

  db:
    image: postgres
    environment:
      POSTGRES_USER: spades
      POSTGRES_PASSWORD: spades
      POSTGRES_DB: spades_dev
    ports:
      - "5432:5432"
    volumes:
      - /var/lib/postgresql/data
volumes:
  client_build:
  server_build:
